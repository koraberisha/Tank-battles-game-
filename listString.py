# This function returns a string of all the index's of an array combined
def ListToString(listo):
    stringo = ""
    for i in range(len(listo)):
        stringo += listo[i]
    return stringo
        
